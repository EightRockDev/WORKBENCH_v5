"""Module A — Skip Trace & POC Intelligence Engine (spec Section 4).

Deterministic owner resolution: registry lookups, vendor APIs, and validation —
no LLM anywhere in the pipeline (Section 11: only the optional plain-English
summary of results is AI, and that is not built here).
"""
