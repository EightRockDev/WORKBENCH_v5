"""Rent-listings scrapers, ported in-workbench 2026-07-30 from
hampton-roads-etl/pullers/listings (the sibling-repo layout never
deployed to the pilot host, so the rent gate had no data source).
The scrapers are verbatim; only the runner is workbench-native
(core/listings_pull.py) - keyed to favorites, polite, freshness-gated."""
