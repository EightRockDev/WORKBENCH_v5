.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
data/workbench.db
workbench.db
*.egg-info/
dist/
build/
.DS_Store
desktop.ini

.env

# --- v5.0 pilot: secrets & runtime (Section 9.1/9.2 — never commit or sync) ---
.streamlit/secrets.toml
*.dump
/backup/
*.db-shm
*.db-wal

# Live autopilot log: held OPEN by the running .bat (Windows locks it),
# so git must never track it - publish() pushes a COPY (autopilot-run.log).
reports/autopilot.log

# NSSM downloaded by install-lan-service.ps1
tools/

# --- Folded-in market ETL (from GRANITE, 2026-08-09) generated data ---
hampton-roads-etl/hampton_roads.db
hampton-roads-etl/**/*.db
hampton_roads.db
data/hampton_roads.db
hampton-roads-etl/.env

# Deal folders live inside the app folder (owner decision 2026-08-15).
# Never commit them: owner names, financials and notes, and this repo is
# pushed to GitHub.
/Properties/
